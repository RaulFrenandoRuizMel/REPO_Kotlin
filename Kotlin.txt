/**
 * You can edit, run, and share this code.
 * play.kotlinlang.org
 */
fun main() {
    
    /*
     * println("Hello, world!!!")
    var edad: Int?;//esto lo que hace es que primete que habra un dato mas adelante
    val impuesto_IVA: Int = 16;
    var hora =16.6; //var hora: Double = 16.6
    hora = "las tres en punto"; //esto no se puede, pues la variable hora ya esta definida como un double, 
    //por lo que para cambiarse se tendria que especificar desde la variable que es esta se traata de un string
    
    //concepto importante de seguridad
    var vive _el_gato_schrodinger: Boolean? = true;//para casos en los que no se sabe si la variable es valida se le pone un "?"
    */
    val nombre: String = "Petra";
    
    var edad :Int? = null;
    val mayoria_edad = 18;
    
    println("${edad ?:0} es el valor actual de edad")
    //println("${if(!(edad > mayoria_edad)){"No"}else {} } es mayor de edad")
    if (edad ?: 0 > mayoria_edad)
    {
        /*
        println(nombre + "Ya Puede chupar")//esto kk
        */
        //println("${if()} Ya Puede chupar")//package
        println("${nombre} Ya Puede chupar")//

    }
    else
    {
        println("Ya puede chupar...nomas que no lo vean los chotas")
    }
    
    //Hacer ejercicio: 
    /*
     * escribir 5 variables de tipo implicitas (Sin doble punto y sin nombre)
     * escribir 5 variales de tipo explicitas (Doble punto y nombre)
     * */
}